<?php
  if ($this->CheckLogin("read") && $this->CheckConfig()) {
    $path=$this->make_path($path);
    if (!$path || !$this->exists($path)) {
      $path=$this->path;
    }
?><html>
<head>
<title><?php echo $title; ?></title>
<script language="javascript">
<!--
	CurrentPath='<?php echo $path; ?>';
	CurrentViewMode='<?php echo $mode; ?>';
	CurrentFilter='<?php echo $filter; ?>';

	function View(path) {
		CurrentPath=path;
		top.browsemiddle.rightnav.location="<?php
		echo $this->store->root; ?>"+path+"browse.nav."+
			CurrentViewMode+".phtml?filter="+escape(CurrentFilter);

		top.browsemiddle.leftnav.location="<?php
		echo $this->store->root; ?>"+path+"browse.nav.phtml";
		top.browsebottom.selectform.filename.value=path;
		top.browsebottom.showFilename();
	}

	function ViewLoaded(path,name) {
		if (top.browsetop && top.browsetop.loaded) {
			top.browsetop.View(path,name);
		}
	}

	function ViewMode(mode) {
		CurrentViewMode=mode;
		View(CurrentPath);
	}

	function New() {
		top.browsemiddle.rightnav.location="<?php
		echo $this->store->root; ?>"+CurrentPath+"new.phtml?returnpage="+
		"<?php echo $this->store->root; ?>"+
		CurrentPath+"browse.nav."+CurrentViewMode+".phtml";
	}

	function filter(type) {
		CurrentFilter=type;
		View(CurrentPath);
	}
//-->
</script>
<style>
	FRAMESET, FRAME {
		BORDER: 0;
	}
</style>
</head>
<frameset rows="30,*,70" border="0">
	<frame name="browsetop" border="0" marginwidth="0" marginheight="0" scrolling="no" frameborder="0" 
		src="<?php echo $this->store->root.$path; ?>browse.top.phtml?filter=<?php echo RawUrlEncode($filter); ?>">
	<frame name="browsemiddle" border="0" frameborder="0" 
		src="<?php echo $this->store->root.$path; ?>browse.middle.phtml?filter=<?php echo RawUrlEncode($filter); ?>">
	<frame name="browsebottom" border="0" frameborder="0" marginwidth="0" marginheight="0" border="0" scrolling="no" 
		src="<?php echo $this->store->root.$path; ?>browse.bottom.phtml?wgBrowseFilename=<?php echo RawUrlEncode($filename); ?>&wgBrowseFilter=<?php echo RawUrlEncode($filter); ?>">
</frameset>
<?php
  }
?>