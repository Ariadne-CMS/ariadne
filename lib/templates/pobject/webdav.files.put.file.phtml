<?php
	if (!$this->exists($file['name'])) {
		debug("putting new file");
		$this->call('ftp.files.save.new.phtml', $arCallArgs);
	} else {
		debug("putting existing file ".$file['name']);
		$this->get($file['name'], 'ftp.files.save.phtml', $arCallArgs);
	}
?>