<?php

	if (!function_exists('addType')) {
		function addType($type, $name, $parents, $children, $icon) {
		global $ARCurrent;
			if (!$ARConfig->arTypeTree[$type]) {
				$ARConfig->arTypeTree[$type]=Array();
			}
			if (is_array($parents)) {
				foreach($parents as $parent) {
					$ARConfig->arTypeTree[$parent][$type]=$name;
				}
			} else if (is_string($parents)) {
				$ARConfig->arTypeTree[$parents][$type]=$name;
			}
			if (is_array($children)) {
				foreach($children as $child => $name) {
					$ARConfig->arTypeTree[$type][$child]=$name;
				}
			}
			$ARConfig->arTypeIcons[$type]=$icon;
		}
	}

	$ARCurrent->arTypeIcons=Array(
		'paddressbook' 	=> $AR->dir->images.'icons/paddressbook_open.gif',
		'particle'		=> $AR->dir->images.'icons/particle_open.gif',
		'pbookmark'		=> $AR->dir->images.'icons/pbookmark_open.gif',
		'pcalendar' 	=> $AR->dir->images.'icons/pcalendar_open.gif',
		'pcalitem' 		=> $AR->dir->images.'icons/pcalitem_open.gif',
		'pdir' 			=> $AR->dir->images.'icons/pdir_open.gif',
		'pfile' 		=> $AR->dir->images.'icons/pfile_open.gif',
		'pgroup' 		=> $AR->dir->images.'icons/pgroup_open.gif',
		'pnewspaper' 	=> $AR->dir->images.'icons/pnewspaper_open.gif',
		'pobject' 		=> $AR->dir->images.'icons/pobject_open.gif',
		'ppage' 		=> $AR->dir->images.'icons/ppage_open.gif',
		'pperson' 		=> $AR->dir->images.'icons/pperson_open.gif',
		'pphoto' 		=> $AR->dir->images.'icons/pphoto_open.gif',
		'pphotobook' 	=> $AR->dir->images.'icons/pphotobook_open.gif',
		'pscenario'		=> $AR->dir->images.'icons/pscenario_open.gif',
		'pshortcut'		=> $AR->dir->images.'icons/pshortcut_open.gif',
		'psite' 		=> $AR->dir->images.'icons/psite_open.gif',
		'puser' 		=> $AR->dir->images.'icons/puser_open.gif',
		'porganization'	=> $AR->dir->images.'icons/porganization_open.gif',
		'pprofile'		=> $AR->dir->images.'icons/pprofile_open.gif',
		'psearch'		=> $AR->dir->images.'icons/psearch_open.gif',
		'psection' 		=> $AR->dir->images.'icons/psection_open.gif'
	);
			
	$list=$this->ls('/system/ariadne/types/','system.get.phtml');
	if (is_array($list)) {
		foreach($list as $type) {
			$ARCurrent->arTypeTree[$type->data->value]=Array();
			$children=$this->ls('/system/ariadne/typetree/normal/'.$type->data->value.'/','system.get.phtml');
			if (is_array($children)) {
				foreach($children as $child) {
					$ARCurrent->arTypeTree[$type->data->value][$child->data->value]=$child->nlsdata->name;
				}
			}
		}
	}
	
	if ($this->CheckLogin('read') && $this->CheckConfig($arCallFunction, $arCallArgs)) {
	}
	
	$arResult=$ARCurrent->arTypeTree;
?>