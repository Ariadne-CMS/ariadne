<?php
	if ($this->CheckLogin("config")) {
		while(list($grant,$val)=each($grants)) {
			$this->data->grants[$type][$id][$grant]=$val;
				display("		$id : $grant\n");
		}
		$this->save();
		$arResult=true;
	}
?>