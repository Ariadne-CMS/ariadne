<?php
	$result["date"]=$this->lastchanged;
	$result["size"]=strlen($nlsdata->page);
	$result["path"]=$this->path;
	$result["type"]="file";

	if ($this->CheckLogin("read")) {
		$grants["read"]=true;
	} else {
		$grants["read"]=false;
	}

	if ($this->CheckLogin("edit")) {
		$grants["write"]=true;
	} else {
		$grants["write"]=false;
	}

	$result["grants"]=$grants;

	$arResult=$result;
?>
