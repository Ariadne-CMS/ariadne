<?php
	debug("CHECKPASSWORD: ".($this->CheckPassword($ARPassword) ? "true" : "false"));
	if ((!$user->data->config || !$user->data->config->disabled) && $this->CheckPassword($ARPassword)) {
		$arResult = $this;
	}
?>
