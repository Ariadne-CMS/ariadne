<?php
	$authconfig = array(
		"userdirs" => array(
			"/system/users/"
		),
		"groupdirs" => array(
			"/system/groups/"
		)
	);

	if (!$this->CheckConfig("auth.ini")) {
		if (is_array($ARCurrent->arResult)) {
			$authconfig = array_merge($authconfig, $ARCurrent->arResult);
		}
	}

	$arResult = $authconfig;
?>