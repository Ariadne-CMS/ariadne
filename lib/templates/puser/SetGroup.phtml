<?php
  debug($this->type."/SetGroup.phtml: $action $path","class");
  if ($action=="add") {
    $data->groups[$path]=$id;
    $this->save($this->path, $this->type, $data);
  } else {
    unset($data->groups[$path]);
    $this->save($this->path, $this->type, $data);
  }
?>