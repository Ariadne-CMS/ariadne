<?php
	if ($this->CheckLogin("read") && $this->CheckConfig()) {
		if ($this->implements('ppage')) {
			$this->call("show.page.html", $arCallArgs);
		} else {
			$this->call("browse.nav.list.phtml", $arCallArgs);
		}
	}
?>