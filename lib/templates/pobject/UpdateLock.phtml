<?php
	global $AR;
	if ($this->CheckLogin("edit")) {

		include($this->store->code."includes/lock.phtml");
		if (!$arResult) {
			$error=$AR->user->data->login.", Can't update lock on ".$this->path;
		}
	} else $error="You have no permission to update this lock";
?>
<script language="javascript">
<?php if ($error) echo "alert('".AddSlashes($error)."');\n"; ?>
window.close();
</script>
