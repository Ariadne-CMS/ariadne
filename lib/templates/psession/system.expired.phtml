<?php
	/******************************************************************
	 system.expired.phtml                                  Muze Ariadne
	 ------------------------------------------------------------------
	 Arguments: none
	 Grant needed: none
	 Global variables: none

	 This template returns true if this session has expired. false 
	 otherwise.

	******************************************************************/

	if (!$valid) {
		$valid=time()-$ARCurrent->session->timeout;
	}
	if ($this->lastchanged < $valid) {
		$arResult=true;
	} else {
		$arResult=false;
	}
?>
