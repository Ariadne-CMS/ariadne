<?php
	$allnls = $ARCurrent->allnls;
	$nolangcheck = $ARCurrent->nolangcheck;	
	$ARCurrent->nolangcheck = true;
	$ARCurrent->allnls = true;
	if (!$this->CheckConfig('config.ini')) {
		// defensive code, only executed when calling config.ini directly
		if (!isset($ARCurrent->arResult)) {
			$ARCurrent->arResult = $ARCurrent->arConfig;
		}
	}

	$ARCurrent->allnls = $allnls;
	$ARCurrent->nolangcheck = $nolangcheck;
?>