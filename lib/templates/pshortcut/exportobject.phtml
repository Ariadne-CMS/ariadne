<?php
	$ARCurrent->nodes[$this->path]=$this->id;
	$ARCurrent->objects[$this->id]["data"]=$this->data;
	$ARCurrent->objects[$this->id]["type"]=$this->type;
	$result=$this->get($this->data->path, "GetType.phtml");
	if ($result && $result[0]) {
		$vtype=$result[0];
		$ARCurrent->objects[$this->id]["vtype"]=$vtype;
	}
	$ARCurrent->objects[$this->id]["properties"]=$this->load_properties();
?>