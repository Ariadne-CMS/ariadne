<?php
	# HTML editor widget ini file

	# set up defaults:
	# hyperlink types en templates
	$arEditorSettings["hyperlink"]["external"]="edit.object.html.link.external.html";
	$arEditorSettings["hyperlink"]["internal"]="edit.object.html.link.internal.html";
	$arEditorSettings["hyperlink"]["indirect"]="edit.object.html.link.indirect.html";

	# affiliate sites
	$arEditorSettings["affiliates"]["muze"]="http://www.muze.nl/";
	$arEditorSettings["affiliates"]["ariadne"]="http://ariadne.muze.nl/";

	# affiliate prefix (template to use)
	$arEditorSettings["affiliate_prefix"]="show.indirect.html?url=";

    # default image directory
	$arEditorSettings["photobook"]["location"]=$this->parent;

	$ARCurrent->arEditorSettings=$arEditorSettings;

	# allow user defined overrides and extensions
	if ($this->CheckLogin("read") && $this->CheckConfig($arCallFunction, $arCallArgs)) {

	}

	$arResult=$ARCurrent->arEditorSettings;
?>