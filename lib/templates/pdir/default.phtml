<?php
  // template not found, try ancestor
  $arCallTemplate=$this->store->code."templates/ppage/".$arCallFunction;
  if (file_exists($arCallTemplate)) {
    include($arCallTemplate);
  } else {
    include($this->store->code."templates/ppage/default.phtml");
  }
?>