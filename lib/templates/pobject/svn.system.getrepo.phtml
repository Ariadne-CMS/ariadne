<?php
	if ($this->CheckLogin("layout") && $this->CheckConfig()) {
		$this->resetloopcheck();
		if ($this->data->svn) {
			$repository = $this->data->svn;
			$repoPath = $this->path;
			$this->putvar("repository" , $repository);
			$this->putvar("repoPath" , $repoPath);
		}
	}
?>