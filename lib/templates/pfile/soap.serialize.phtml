<?php
  if ($this->CheckSilent("read") && $this->CheckConfig()) {
	$result = $this->call("pobject::soap.serialize.phtml", $arCallArgs);
	$result["mimetype"] = $this->data->mimetype;
	$files = $this->store->get_filestore("files");
	$result["file"] = base64_encode($files->read($this->id, "file"));
	$arResult = $result;
  }
?>