<?php
	debug("CHECKPASSWORD: ".($this->CheckPassword($ARPassword) ? "true" : "false"));
	if ((!$this->data->config || !$this->data->config->disabled) && $this->CheckPassword($ARPassword)) {
		$arResult = $this;
	}
?>