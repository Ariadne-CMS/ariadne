<?php
	function merge(&$a,$b) {
		if ((gettype($a)==gettype($b)) && (is_array($b) || is_object($b))) {
			while (list($key,$val)=each($b)) {
				if (is_object($b)) {
					merge($a->$key,$val);
				} else {
					merge($a[$key],$val);
				}
			}
		} else {
			$a=$b;
		}
	}
?>