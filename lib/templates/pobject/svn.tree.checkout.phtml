<div style="width: 100%; height: 90%; overflow: auto">
<pre>
<?php
        if ($this->CheckLogin("layout") && $this->CheckConfig()) {

		$repository = $_POST['repository'];
		$username = $_POST['username'];
		$password = $_POST['password'];

		if (! (isset($repository) && isset($username) && isset($password)) ) {
			?>
				<form method="post">
					SVN repo: <input type="text" name="repository">
					SVN user: <input type="text" name="username">
					SVN pass: <input type="password" name="password">
					<input type="submit" value="checkout recursive">
				</form>
			<?php
		} else {
			echo "Doing recursive checkout...";
			set_time_limit(0);
			$this->call("svn.system.checkout.recursive.phtml", $arCallArgs);
		}
	}
?>
</pre>
</div>