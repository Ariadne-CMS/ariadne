<?php
	if ($this->CheckLogin("config")) {
		while (list($type, $ids)=each($ARCurrent->importGrants)) {
			while (list($id, $grants)=each($ids)) {
				while(list($grant,$val)=each($grants)) {
					$this->data->grants[$type][$id][$grant]=$val;
					echo "$id : $grant\n";
				}
			}
		}
		$arResult=true;
	}
?>