<?php
	if (!$setDummyObject) {
		$this->properties=$this->load_properties();
		$this->call("system.export.get.grants.phtml");
	} else {
		$this->dummy_save=1;
	}

	/* 
		unset grants defined on this object, they will be resetted after
		the complete database is imported
	*/
	unset($this->data->grants);
	unset($this->data->usergrants);
	$arResult=$this;
?>