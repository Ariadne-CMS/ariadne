<?php
	if ($this->CheckLogin("config")) {
		foreach ($grants as $grant => $val) {
			$this->data->config->grants[$type][$id][$grant]=$val;
				display("		$id : $grant\n");
		}
		$this->save();
		$arResult=true;
	}
?>
