<?php
	# HTML editor widget ini file

	# set up defaults:
	# hyperlink types en templates
	$arResult["hyperlink"]["external"]="edit.object.html.link.external.html";
	$arResult["hyperlink"]["internal"]="edit.object.html.link.internal.html";
	$arResult["hyperlink"]["indirect"]="edit.object.html.link.indirect.html";

	# affiliate sites
	$arResult["affiliates"]["muze"]="http://www.muze.nl/";
	$arResult["affiliates"]["ariadne"]="http://ariadne.muze.nl/";

	# affiliate prefix (template to use)
	$arResult["affiliate_prefix"]="show.indirect.html?url=";

	# allow user defined overrides and extensions
	if ($this->CheckLogin("read") && $this->CheckConfig($arCallArgs, $arCallFunction)) {

	}
?>